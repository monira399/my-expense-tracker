import 'package:flutter/cupertino.dart';

class ExpenseIncomeToggleBar extends StatelessWidget {
  const ExpenseIncomeToggleBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        Container(

        )
      ],
    );
  }
}
